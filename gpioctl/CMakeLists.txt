cmake_minimum_required(VERSION 2.8)
project(gpioctl)
add_executable(gpioctl gpioctl.c)
add_definitions(-I${CMAKE_SOURCE_DIR} -L${CMAKE_SOURCE_DIR})
add_dependencies(gpioctl libgpio)
target_link_libraries(gpioctl gpio)
