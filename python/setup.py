#!/usr/bin/env python

from distutils.core import setup

setup(name='GPIO',
      version='0.1',
      description='FreeBSD GPIO library',
      author='Rui Paulo',
      author_email='rpaulo@felyko.com',
      packages=['GPIO'])
